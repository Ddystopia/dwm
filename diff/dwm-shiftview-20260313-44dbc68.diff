Description
===========
Shift the view to the next or previous tag, wrapping around.

From dwm-shiftview-6.4.diff, whose hunk headers are miscounted the same way
as pwkl and are recomputed here.

Applies to
==========
dwm git master, commit 44dbc68 (2026-03-13)

diff a/dwm.c b/dwm.c
--- a/dwm.c	2026-03-13 18:27:18.000000000 +0100
+++ b/dwm.c	2026-08-23 13:09:23.467998894 +0200
@@ -201,6 +201,8 @@
 static void setfullscreen(Client *c, int fullscreen);
 static void setlayout(const Arg *arg);
 static void setmfact(const Arg *arg);
+static void shiftview(const Arg *arg);
+static void shiftview_to_not_empty(const Arg *arg);
 static void setup(void);
 static void seturgent(Client *c, int urg);
 static void showhide(Client *c);
@@ -1626,6 +1628,64 @@
 	XFree(wmh);
 }
 
+/** Function to shift the current view to the left/right
+ *
+ * @param: "arg->i" stores the number of tags to shift right (positive value)
+ *          or left (negative value)
+ */
+void
+shiftview(const Arg *arg) {
+       Arg shifted;
+
+       if(arg->i > 0) // left circular shift
+               shifted.ui = (selmon->tagset[selmon->seltags] << arg->i)
+                  | (selmon->tagset[selmon->seltags] >> (LENGTH(tags) - arg->i));
+
+       else // right circular shift
+               shifted.ui = selmon->tagset[selmon->seltags] >> (- arg->i)
+                  | selmon->tagset[selmon->seltags] << (LENGTH(tags) + arg->i);
+
+       view(&shifted);
+}
+
+void
+shiftview_to_not_empty(const Arg *arg) {
+  int i;
+  unsigned int tagset;
+  Client *c;
+  Arg shifted;
+
+  // function runs only when single tag is viewed
+  for (tagset = selmon->tagset[selmon->seltags]; tagset; tagset <<= 1){
+    if ((tagset & (TAGMASK - 1)) != 0 && (tagset & (TAGMASK - 1)) != tagset){
+      return;
+    }
+  }
+
+  i = arg->i;
+  tagset = selmon->tagset[selmon->seltags];
+
+  while (-(int)LENGTH(tags) < i && i < (int)LENGTH(tags)) {
+    if(arg->i > 0) { // left circular shift
+      shifted.ui = (tagset << i) | (tagset >> (LENGTH(tags) - i));
+      i++;
+    } else { // right circular shift
+      shifted.ui = tagset >> (- i) | tagset << (LENGTH(tags) + i);
+      i--;
+    }
+    
+    for (c = selmon->stack; c; c = c->snext) {
+      if (tagset & shifted.ui) {
+        view(&shifted);
+        return;
+      }
+    }
+  }
+  // if nothing is found
+  shiftview(arg);
+}
+
+
 void
 showhide(Client *c)
 {
